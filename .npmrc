# @cp packages are published to GitHub Packages by the cp-testt1-09 release workflow.
# CI and developer machines authenticate via GITHUB_TOKEN / a classic PAT with read:packages.
@cp:registry=https://npm.pkg.github.com
